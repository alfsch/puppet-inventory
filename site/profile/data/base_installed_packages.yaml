lookup_options:
  profile::base::packages::ensured_package_list:
    merge: unique
profile::base::packages::ensured_package_list: [
  auto-apt-proxy,
  avahi-utils,
  bash-completion,
  bzip2,
  ca-certificates,
  cifs-utils,
  curl,
  fdisk,
  gdisk,
  gnupg-agent,
  iptraf-ng,
  jq,
  keyutils,
  locate,
  members,
  net-tools,
  nfs-common,
  openssl,
  pwgen,
  rsync,
  screen,
  seccomp,
  smbclient,
  sudo,
  tcpdump,
  unzip,
  vim,
  wget,
]
