version: 5
hierarchy:
- name: installed packages
  paths:
  - base_installed_packages.yaml
- name: purged packages
  paths:
  - base_purged_packages.yaml
- name: unattended upgrades
  paths:
  - base_unattended_upgrades.yaml
- name: common data configuration
  paths:
  - common.yaml
